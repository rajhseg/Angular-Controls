import { GridColumnForDirective } from './grid-column-for.directive';

describe('GridColumnForDirective', () => {
  it('should create an instance', () => {
    const directive = new GridColumnForDirective();
    expect(directive).toBeTruthy();
  });
});
